<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gradient Buttons</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Define custom colors in Tailwind config */
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'dark-navy': '#0b0e27', /* Background color */
                        'button-dark': '#1a1f3c', /* Slightly lighter dark for the button fill */
                        'grad-start': '#c048f0', /* Purple */
                        'grad-end': '#e55c9d',   /* Pink */
                    }
                }
            }
        }

        /* --- Custom CSS for the Gradient Border Effect --- */

        .gradient-border-button {
            /* The parent element holds the gradient border */
            position: relative;
            padding: 2px; /* This padding creates the border thickness */
            border-radius: 50px; /* Heavy rounding for pill shape */
            background: linear-gradient(90deg, theme('colors.grad-start'), theme('colors.grad-end'));
            /* Key for the effect: prevents the background gradient from showing through */
            background-clip: padding-box;
            transition: all 0.3s ease; /* Smooth hover transition */
        }

        .gradient-border-button:hover {
            /* On hover, slightly increase the padding/border size for a 'pop' effect */
            padding: 3px;
        }

        .button-content {
            /* The child element is the dark fill inside the border */
            background-color: theme('colors.button-dark');
            border-radius: 48px; /* Slightly less than the parent to match the border */
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            /* Text styling for the inner content */
            color: white;
            font-weight: 600;
            padding: 12px 30px; /* Internal padding */
        }

        /* An optional subtle hover effect on the content */
        .gradient-border-button:hover .button-content {
            background-color: #2b3259; /* Darken the inner button slightly on hover */
        }

    </style>
</head>

<body class="bg-dark-navy min-h-screen flex items-center justify-center p-6">

    <div class="flex flex-col space-y-6 w-full max-w-sm">

        <button class="gradient-border-button">
            <span class="button-content">
                Browse
            </span>
        </button>

        <button class="gradient-border-button">
            <span class="button-content">
                Krossfire
            </span>
        </button>

        <button class="gradient-border-button">
            <span class="button-content">
                Learn More
            </span>
        </button>

    </div>

</body>
</html>
